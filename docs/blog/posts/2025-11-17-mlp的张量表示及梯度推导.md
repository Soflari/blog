---
date: 2025-11-17
slug: mlp的张量表示及梯度推导
authors:
  - me
source_permalink: 'hugo_dev:p/mlp的张量表示及梯度推导/'
---

# MLP的张量表示及梯度推导

## 代码部分

这是多层感知机（MLP）的一段示例代码

```python
import os
import torch
from torch import nn

class NeuralNetwork(nn.Module):
    def __init__(self):
        super().__init__()
        self.flatten = nn.Flatten()
        self.linear_relu_stack = nn.Sequential(
            nn.Linear(28*28, 512),
            nn.ReLU(),
            nn.Linear(512, 512),
            nn.ReLU(),
            nn.Linear(512, 10),
        )

    def forward(self, x):
        x = self.flatten(x)
        logits = self.linear_relu_stack(x)
        return logits
        
model = NeuralNetwork().to(device)
X = torch.rand(1, 28, 28, device=device)
logits = model(X)
pred_probab = nn.Softmax(dim=1)(logits)
y_pred = pred_probab.argmax(1)
```

## 神经网络的结构

**1. `nn.Flatten()`**
这个函数将输入张量的各维度展平成一个向量。默认从索引1开始到索引-1结束，要注意的是索引是从0开始的，所以索引1其实是指的第2个。
这里的输入X维度为 $[1,28,28]$ ，经过`nn.Flatten()`后应该是 $[1,28*28]$ 。
从张量角度看，

$\displaystyle x_{b1ij}=x_{bn}, \quad n=0*28*28+i*28+j$

**2. `linear_relu_stack`**
这个函数将一系列的神经层排成一个列表，数据将从这些层中流出。过程中会改变维度。

1. `Linear(28*28, 512)`
   张量表示: $y_{bm}=x_{bn}w_{nm}+\mathbf{1}_{b}b_{m}$
2. `ReLU`
   张量表示: $y_{bm}^{r}=\text{ReLU}(y_{bm})$
3. `Linear(512,512)`
   张量表示: $y_{bm'}'=y_{bm}^{r}w_{mm'}+\mathbf{1}_{b}b_{m'}$
4. `ReLU`
   张量表示: $y_{bm'}^{r}=\text{ReLU}(y_{bm'})$
5. `Linear(512,10)`
   张量表示: $y_{bo}=y_{bm'}^{r}w_{m'o}+\mathbf{1}_{b}b_{o}$

**3. `Softmax(dim=1)`**
张量表示：

$\displaystyle s_{bi}= \frac{e^{x_{bi}}}{\mathbf{1}_{kj}e^{x_{kj}}}$

**4. 交叉熵损失**

$\displaystyle c_{b}=-\log{s_{b\text{T}}}$

交叉熵损失是单分类损失函数，这里的 $\text{T}$ 是代表真实标签的常量索引，这里的 $\text{T}$ 不是一种哑指标。

## 推导梯度

### 交叉熵部分梯度

重点是利用反向传播，也就是由终点向起点一层层推。

**1. $c_{b}$**

$\displaystyle \frac{\partial c_{b}}{\partial s_{ki}}=-\frac{\partial \log s_{b\text{T}}}{\partial s_{ki}} =-\frac{1}{s_{k\text{T}}}\delta_{i\text{T}}$

**2. $s_{bi}$**

$\displaystyle \frac{\partial s_{bi}}{\partial y_{ko}} =\frac{\partial \mathbf{1}_{kj}e^{x_{kj}}}{\partial y_{ko}}$

令 $\sigma=\mathbf{1}_{kj}e^{x_{kj}}$ ,

$\displaystyle \frac{\partial \mathbf{1}_{kj}e^{x_{kj}}}{\partial y_{ko}} =\frac{\partial\sigma}{\partial y_{ko}} =\frac{1}{\sigma^2}( \frac{\partial e^{y_{bi}}}{\partial y_{ko}}\sigma-e^{y_{bi}}\frac{\partial \sigma}{\partial y_{ko}})$

**2.1 $\frac{\partial e^{y_{bi}}}{\partial y_{ko}}$**

$\displaystyle \frac{\partial e^{y_{bi}}}{\partial y_{ko}} =e^{y_{bi}}\frac{\partial y_{bi}}{\partial y_{ko}} =e^{y_{bi}}\delta_{bk}\delta_{io}$

**2.2 $\frac{\partial \sigma}{\partial y_{ko}}$**

$\displaystyle \frac{\partial \sigma}{\partial y_{ko}}=\frac{\partial \mathbf{1}_{bj}e^{y_{bj}}}{\partial y_{ko}} =\mathbf{1}_{bj}e^{y_{bj}}\delta_{bk}\delta_{jo}=\mathbf{1}_{ko}e^{y_{ko}} =e^{y_{ko}}$

代入原式：

$\displaystyle \frac{1}{\sigma^2}( \frac{\partial e^{y_{bi}}}{\partial y_{ko}}\sigma-e^{y_{bi}}\frac{\partial \sigma}{\partial y_{ko}}) =\frac{1}{\sigma^2}( e^{y_{bi}}\delta_{bk}\delta_{io}\sigma-e^{y_{bi}}e^{y_{ko}}) =s_{bi}\delta_{bk}\delta_{io}-s_{bi}s_{ko}$

即

$\displaystyle \frac{\partial s_{bi}}{\partial y_{ko}} =s_{bi}\delta_{bk}\delta_{io}-s_{bi}s_{ko}$

综合一下，现在就有

$\displaystyle \frac{\partial c_{b}}{\partial y_{ko}} =\frac{\partial c_{b}}{\partial s_{ki}}\frac{\partial s_{bi}}{\partial y_{ko}} =-\frac{1}{s_{k\text{T}}}\delta_{i\text{T}}(s_{bi}\delta_{bk}\delta_{io}-s_{bi}s_{ko}) =s_{ko}-\delta_{bk}\delta_{To}$

这里可以看出，交叉熵损失在最后一层的梯度，恰好等于第 $k$ 个样本的第 $o$ 个logits(概率值)减去第 $k$ 个样本的第 $\text{T}$ 个标签(也就是1).

### 单个线性层梯度

- 输入  $X$ : (∗, in_features)
- 权重 $A$  : (out_features,in_features)
- 偏置  $b$ : (out_features)
- 输出  $Y$ : (∗, out_features)

PyTorch对线性层的定义：

$$
Y=XA^{T}+b
$$

令 $C=A^{T}$ ，就有张量表示：

$$
y_{ij}=x_{ik}c_{kj}+\mathbf{1}_{i}b_{j}
$$

上式中， $k$ 重复出现代表做了乘积，同时在等式左侧未出现，就还做了求和。 $\mathbf{1}_{i}$ 用于将 $b_{j}$ 广播成矩阵。

根据 [Linear Layer, Deriving the Gradient for the Backward Pass](https://robotchinwag.com/posts/linear-layer-deriving-the-gradient-for-the-backward-pass/) 的结果，我们有如下结论：

**1. 对 $X$ 的梯度**

$\displaystyle \frac{\partial y_{ij}}{\partial x_{pq}}=\delta_{ip}c_{qj}$

$$\begin{aligned}
\frac{\partial l}{\partial x_{pq}} & =\frac{\partial l}{\partial y_{ij}}\frac{\partial y_{ij}}{\partial x_{pq}} \\
 & =\frac{\partial l}{\partial y_{ij}}\delta_{ip}c_{qj} \\
 & =\frac{\partial l}{\partial y_{pj}}c_{qj}
\end{aligned}$$

**2. 对 $A$ 的梯度**

$$\begin{aligned}
	\frac{\partial y_{ij}}{\partial a_{pq}} & =\frac{\partial}{\partial a_{pq}}[x_{ik}c_{kj}+\mathbf{1}_ib_j] \\
	 & =x_{ik}\frac{\partial c_{kj}}{\partial a_{pq}}
	\end{aligned}$$

由于 $C=A^{T}$ ， 所以有 $c_{kj}=a_{jk}$ 。继续有公式：

$$\begin{aligned}
	\frac{\partial y_{ij}}{\partial a_{pq}} & =x_{ik}\frac{\partial a_{jk}}{\partial a_{pq}} \\
	 & =x_{ik}\delta_{jp}\delta_{kq} \\
	 & =x_{iq}\delta_{jp}
	\end{aligned}$$


$$\begin{aligned}
	\frac{\partial l}{\partial a_{pq}} & =\frac{\partial l}{\partial y_{ij}}\frac{\partial y_{ij}}{\partial a_{pq}} \\
	 & =\frac{\partial l}{\partial y_{ij}}x_{iq}\delta_{jp} \\
	 & =\frac{\partial l}{\partial y_{ip}}x_{iq}
	\end{aligned}$$

**3. 对 $b$ 的梯度**

$\displaystyle \begin{aligned} \frac{\partial y_{ij}}{\partial b_p} & =\frac{\partial}{\partial b_p}[x_{ik}c_{kj}+\mathbf{1}_ib_j] \\ & =\frac{\partial\left(\mathbf{1}_ib_j\right)}{\partial b_p} \\ & =1_i\frac{\partial b_j}{\partial b_p} \end{aligned}$

$\displaystyle \frac{\partial y_{ij}}{\partial b_p}=\mathbf{1}_i\delta_{jp}$

$\displaystyle \begin{aligned} \frac{\partial l}{\partial b_p} & =\frac{\partial l}{\partial y_{ij}}\frac{\partial y_{ij}}{\partial b_p} \\ & =\frac{\partial l}{\partial y_{ij}}\mathbf{1}_i\delta_{jp} \\ & =\frac{\partial l}{\partial y_{ip}}\mathbf{1}_i \end{aligned}$
